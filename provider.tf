provider "aws" {
  region = "us-east-1"

  # Optional - access key and secret key
  access_key = "YOUR_ACCESS_KEY" 
  secret_key = "YOUR_SECRET_KEY"

  # Optional - profile name from credential file
  profile = "default"
}
